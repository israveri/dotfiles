[user]
    name            = Israel Ribeiro
    email           = israveri@gmail.com

[core]
    editor          = nvim
    excludesfile    = ~/.config/git/gitignore
    pager           = bat --plain --theme=ansi --paging=never

[push]
    default         = simple

[color]
    branch          = auto
    diff            = auto
    status          = auto
    ui              = auto

[color "status"]
    added           = green bold
    changed         = yellow bold
    untracked       = red bold

[pager]
    diff            = bat --plain --theme=ansi --paging=always

[pull]
    rebase          = true

[merge]
    ff              = true
    tool            = nvimdiff

[mergetool]
    prompt          = false

[mergetool "nvimdiff"]
    cmd             = "nvim -d \"$LOCAL\" \"$MERGED\" \"$REMOTE\""

[diff]
    tool            = nvimdiff

[difftool]
    prompt          = false

[difftool "nvimdiff"]
    cmd             = "nvim -d \"$LOCAL\" \"$REMOTE\""


[init]
    defaultBranch   = main
