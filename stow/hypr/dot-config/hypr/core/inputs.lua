hl.config({
    input = {
        kb_layout = "us",
        kb_variant = "intl",
        kb_model = "",
        kb_options = "",
        kb_rules = "",

        follow_mouse = 0,
        repeat_rate = 35,
        repeat_delay = 200,
        sensitivity = 0.5,
    },

    cursor = {
        no_warps = true,
    }
})
