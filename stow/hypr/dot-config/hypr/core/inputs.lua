hl.config({
    input = {
        kb_layout = "us",
        kb_variant = "intl",

        follow_mouse = 0,
        repeat_rate = 50,
        repeat_delay = 400,
        sensitivity = 0.5,

        accel_profile = "flat"
    },

    cursor = {
        no_warps = true,
        inactive_timeout = 5,
    }
})
